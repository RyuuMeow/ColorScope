/**
 * FilterTool — Handles filter activation
 * Note: Hue/Harmony filter controls are now rendered in the Properties panel.
 * This class is retained for any future filter-specific logic.
 */
import { bus } from '../utils/EventBus.js';

export class FilterTool {
  constructor() {
    // Filter controls are now managed by the Properties panel in main.js
    // This class is kept as a placeholder for filter-related event handling
  }
}
